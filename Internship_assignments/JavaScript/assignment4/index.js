const widthCointainer =  500;
const heightCointainer =  500;

function createElement(element, className = null, id = null, targetName){
    var element = document.createElement(element);
    element.className= className
    document.querySelector(targetId).appendChild(element);
}

function main(){
    
}

main();